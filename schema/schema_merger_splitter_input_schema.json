{
  "$id": "schema_merger_splitter_input_schema.json",
  "title": "Schema Merger–Splitter Input Schema",
  "type": "object",
  "additionalProperties": false,

  "properties": {

    "output_filename": {
      "type": "string",
      "description": "File name (not path) where the merged JSON must be written inside the simulators/ directory."
    },

    "sources": {
      "type": "object",
      "description": "Mapping of source filenames to their extraction rules. Each key must be a file name inside the simulators/ directory.",
      "additionalProperties": {
        "type": "array",
        "description": "List of field extraction rules for this source file.",
        "items": {
          "type": "object",
          "additionalProperties": false,

          "properties": {
            "from": {
              "type": "string",
              "description": "JSONPath expression pointing to the field inside the source JSON."
            },
            "to": {
              "type": "string",
              "description": "Top-level key name in the merged JSON where the extracted value will be placed."
            }
          },

          "required": ["from", "to"]
        }
      }
    }
  },

  "required": ["output_filename", "sources"]
}